# postgresql.conf common settings

listen_addresses: '*'
wal_level: 'hot_standby'
max_wal_senders: 6
wal_keep_segments: 256
hot_stanby: 'on'


# Bollean variable to indicate if there's been a failover. Slave is rebuilt if it is set to true

failover: True
